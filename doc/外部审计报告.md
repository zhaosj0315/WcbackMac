# 外部审计报告

审计日期：2026-04-19  
审计范围：`readme.md`、`doc/*.md` 全部材料  
代码状态：封板，不可更改

---

## 1. 问题清单

| 编号 | 文件 | 问题描述 | 严重度 | 处理 |
|------|------|----------|--------|------|
| P-01 | `doc/开发者手册.md` | 前半段全是 Windows 安装步骤（clone LC044/WeChatMsg、MSVC、`python main.py`），与本项目无关 | 高 | 已重写 |
| P-02 | `doc/开发者手册.md` | 调试命令路径用 `app/Database/MacMsg`，主脚本实际默认 `app/DataBase/MacMsg`（大写 B） | 高 | 已修正 |
| P-03 | `doc/开发者手册.md` | 推荐 `mac_export_messages.py`、`mac_export_html.py` 作为主流程，设计文档已标注这些为"可废弃" | 高 | 已修正 |
| P-04 | `doc/Mac版本导出系统设计.md` | 1.2 节含个人数据规模（1,300,493 条、33,197 联系人等），属私有信息，不应出现在公开文档 | 高 | 已删除，改为通用描述 |
| P-05 | `doc/Mac版本导出系统设计.md` | 1.3 节"当前问题"描述历史状态（脚本碎片化、路径混乱），多数已修复 | 中 | 已改为"已知限制" |
| P-06 | `doc/Mac版本导出系统设计.md` | 6.2 节命令含 `--export-output data/mac_messages.csv`，该参数不是当前主流程 | 中 | 已删除该参数 |
| P-07 | `doc/Mac版本导出系统设计.md` | 目标输出结构含个人数据量（171,058 张图片等） | 中 | 已删除 |
| P-08 | `doc/Mac版本使用指南.md` | 步骤 3.3/3.4/3.5 引用 `mac_export_messages.py`、`mac_export_all.py`，这些是旧入口 | 高 | 已重写 |
| P-09 | `doc/Mac版本使用指南.md` | 路径写 `app/Database/MacMsg`，主脚本实际默认 `app/DataBase/MacMsg` | 高 | 已修正 |
| P-10 | `doc/文档开发管理规范.md` | 文档分类表列了已删除的 `Mac版本功能总结.md`、`Mac版本可行性与实施.md`、`材料清理报告.md` | 中 | 已更新分类表 |
| P-11 | `doc/文档开发管理规范.md` | 路径规范写 `app/Database/MacMsg`，与实际脚本默认值不符 | 中 | 已修正 |

---

## 2. 修正动作

| 文件 | 动作 |
|------|------|
| `doc/开发者手册.md` | 完全重写：去掉 Windows 内容，以 Mac 主流程为主，修正路径和脚本引用 |
| `doc/Mac版本导出系统设计.md` | 删除个人数据规模，修正已知限制描述，修正 6.2 命令，修正输出结构中的数量 |
| `doc/Mac版本使用指南.md` | 完全重写：以 `mac_export_by_session.py` 为主入口，修正路径，补充已知限制 |
| `doc/文档开发管理规范.md` | 更新文档分类表（删除已清理文档条目），修正路径规范 |

---

## 3. 验证记录

- 所有文档引用的脚本均通过 `ls scripts/mac_*.py` 确认存在
- 主脚本接口通过 `--help` 验证：`mac_export_by_session.py`、`mac_web_server.py`、`mac_contact_mapper.py`、`mac_auto_decrypt_export.py`
- 默认路径通过 `grep "default.*DataBase\|default.*Database"` 确认为 `app/DataBase/MacMsg`
- 私有信息（个人数据规模、wxid）已从所有文档中清除

---

## 4. 遗留风险（代码封板，文档标注）

| 风险 | 说明 |
|------|------|
| emoji/music/icon 目录为空 | 代码创建目录骨架但不填充，文档已标注"当前不填充" |
| 朋友圈无图片 | 只导出 XML，图片需从 CDN 下载，未实现，文档已标注 |
| 无增量更新 | 每次全量重跑，文档已标注 |
| 解密依赖 LLDB 权限 | 部分环境可能失败，文档已标注"实验性" |
| mac_contact_mapper.py 默认路径 | 该脚本默认 `app/Database/MacMsg`（小写 b），与其他脚本不一致，需手动指定 `--db-dir app/DataBase/MacMsg` |
